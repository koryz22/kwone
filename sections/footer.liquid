{% comment %}
  Section: footer.liquid
  Purpose: Professional footer with email collection and links
{% endcomment %}

<footer class="site-footer" role="contentinfo">
  <div class="footer-wrapper">
    
    {% comment %} Newsletter Signup Section {% endcomment %}
    {% if section.settings.show_newsletter %}
      <div class="footer-newsletter">
        <h3 class="newsletter-title">{{ section.settings.newsletter_title | default: 'JOIN OUR NEWSLETTER' }}</h3>
        <form class="newsletter-form" id="newsletter-form" action="/contact" method="post" accept-charset="UTF-8">
          <input type="hidden" name="form_type" value="contact">
          <input type="hidden" name="utf8" value="✓">
          
          <div class="newsletter-input-wrapper">
            <input 
              type="email" 
              name="contact[email]" 
              id="newsletter-email" 
              class="newsletter-input" 
              placeholder="{{ section.settings.email_placeholder | default: 'EMAIL ADDRESS' }}"
              required
              aria-label="Email address"
            >
            <button type="submit" class="newsletter-submit" id="newsletter-submit">
              {{ section.settings.subscribe_button | default: 'SUBSCRIBE' }}
            </button>
          </div>
          
          <div class="newsletter-message" id="newsletter-message" style="display: none;"></div>
          
          {% if section.settings.show_disclaimer %}
            <p class="newsletter-disclaimer">{{ section.settings.disclaimer_text }}</p>
          {% endif %}
        </form>
      </div>
    {% endif %}

    {% comment %} Footer Content Grid - Centered Brand Text {% endcomment %}
    <div class="footer-content-grid">
      <div class="footer-brand-centered">
        {% if section.settings.brand_description != blank %}
          <p class="footer-brand-text">{{ section.settings.brand_description }}</p>
        {% endif %}
        {% if section.settings.brand_tagline != blank %}
          <p class="footer-brand-tagline">{{ section.settings.brand_tagline }}</p>
        {% endif %}
      </div>
    </div>

    {% comment %} Footer Links {% endcomment %}
    <div class="footer-links">
      {% if section.settings.show_footer_links %}
        <div class="footer-links-row">
          {% if section.settings.privacy_policy_url != blank %}
            <a href="{{ section.settings.privacy_policy_url }}" class="footer-link">{{ section.settings.privacy_policy_label | default: 'PRIVACY POLICY' }}</a>
          {% endif %}
          {% if section.settings.shipping_url != blank %}
            <a href="{{ section.settings.shipping_url }}" class="footer-link">{{ section.settings.shipping_label | default: 'SHIPPING & RETURNS' }}</a>
          {% endif %}
        </div>
      {% endif %}
    </div>

    {% comment %} Copyright {% endcomment %}
    <div class="footer-bottom">
      <p class="footer-copyright">
        &copy; {{ 'now' | date: '%Y' }} {{ section.settings.copyright_text | default: shop.name | upcase }}
      </p>
      {% if section.settings.show_site_credit %}
        <p class="footer-credit">{{ section.settings.site_credit_text }}</p>
      {% endif %}
    </div>

  </div>
</footer>

<style>
  .site-footer {
    padding: var(--spacing-xxl) 0 var(--spacing-lg);
    border-top: none;
    margin-top: var(--spacing-xxl);
  }

  .footer-wrapper {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--spacing-xl);
  }

  /* Newsletter Section */
  .footer-newsletter {
    margin-bottom: var(--spacing-lg);
    padding-bottom: var(--spacing-lg);
    border-bottom: none;
    text-align: center;
  }

  .newsletter-title {
    font-size: 1.4rem;
    font-weight: 300;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: var(--spacing-md);
    color: var(--color-text);
    font-family: var(--font-sans);
  }

  .newsletter-form {
    max-width: 600px;
    margin: 0 auto;
  }

  .newsletter-input-wrapper {
    display: flex;
    gap: var(--spacing-sm);
    margin-bottom: var(--spacing-sm);
  }

  .newsletter-input {
    flex: 1;
    background: transparent;
    border: 1px solid rgba(255, 255, 255, 0.2);
    padding: 1rem 1.5rem;
    color: var(--color-text);
    font-size: 1.1rem;
    font-family: var(--font-sans);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    transition: border-color 0.3s ease;
  }

  .newsletter-input::placeholder {
    color: var(--color-text-muted);
    opacity: 0.6;
  }

  .newsletter-input:focus {
    outline: none;
    border-color: rgba(255, 255, 255, 0.4);
  }

  .newsletter-submit {
    background: #1e4a12;
    color: var(--color-text);
    border: 1px solid #1a3d0e;
    padding: 1rem 2rem;
    font-size: 1.1rem;
    font-family: var(--font-sans);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-weight: 400;
    cursor: pointer;
    transition: all 0.3s ease;
    white-space: nowrap;
  }

  .newsletter-submit:hover {
    background: #2d5a1a;
    border-color: #2d5a1a;
    color: var(--color-text);
  }

  .newsletter-message {
    margin-top: var(--spacing-sm);
    font-size: 0.85rem;
    padding: var(--spacing-sm);
  }

  .newsletter-message.success {
    color: #4ade80;
  }

  .newsletter-message.error {
    color: #f87171;
  }

  .newsletter-disclaimer {
    font-size: 0.9rem;
    color: var(--color-text-muted);
    line-height: 1.6;
    font-family: var(--font-mono);
    margin-top: var(--spacing-sm);
    max-width: 600px;
  }

  .newsletter-disclaimer a {
    text-decoration: underline;
    color: var(--color-text-muted);
  }

  /* Footer Content Grid */
  .footer-content-grid {
    display: flex;
    justify-content: center;
    align-items: center;
    margin-bottom: var(--spacing-lg);
    padding-bottom: var(--spacing-lg);
    border-bottom: none;
  }

  .footer-brand-centered {
    text-align: center;
    max-width: 800px;
  }

  .footer-brand-text {
    font-size: 1.2rem;
    line-height: 1.8;
    color: var(--color-text);
    font-family: var(--font-mono);
    letter-spacing: 0.02em;
    margin-bottom: var(--spacing-sm);
  }

  .footer-brand-tagline {
    font-size: 1.2rem;
    line-height: 1.8;
    color: var(--color-text-muted);
    font-family: var(--font-mono);
    font-style: italic;
  }

  .footer-logo-wrapper {
    margin-bottom: var(--spacing-sm);
  }

  .footer-logo-image {
    max-width: 150px;
    height: auto;
  }

  .footer-location {
    font-size: 0.75rem;
    color: var(--color-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-family: var(--font-sans);
  }

  .footer-social-title {
    font-size: 1.1rem;
    font-weight: 400;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: var(--spacing-xs);
    color: var(--color-text);
    font-family: var(--font-sans);
  }

  .footer-social-link {
    font-size: 1rem;
    color: var(--color-text);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    transition: opacity 0.3s ease;
    font-family: var(--font-sans);
  }

  .footer-social-link:hover {
    opacity: 0.6;
  }

  /* Footer Links */
  .footer-links {
    margin-bottom: var(--spacing-lg);
    text-align: center;
  }

  .footer-links-row {
    display: flex;
    gap: var(--spacing-lg);
    flex-wrap: wrap;
    justify-content: center;
  }

  .footer-link {
    font-size: 1.1rem;
    color: var(--color-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-family: var(--font-mono);
    transition: color 0.3s ease;
  }

  .footer-link:hover {
    color: var(--color-text);
  }

  /* Footer Bottom */
  .footer-bottom {
    text-align: center;
    padding-top: var(--spacing-lg);
  }

  .footer-copyright {
    font-size: 1.1rem;
    color: var(--color-text-muted);
    font-family: var(--font-mono);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: 0;
  }

  .footer-credit {
    font-size: 0.9rem;
    color: var(--color-text-muted);
    font-family: var(--font-mono);
    opacity: 0.6;
  }

  /* Responsive */
  @media (max-width: 968px) {
    .footer-brand-centered {
      max-width: 100%;
      padding: 0 var(--spacing-sm);
    }

    .newsletter-input-wrapper {
      flex-direction: column;
    }

    .newsletter-submit {
      width: 100%;
    }
  }

  @media (max-width: 768px) {
    .site-footer {
      padding: var(--spacing-lg) var(--spacing-sm) var(--spacing-md);
    }

    .footer-newsletter {
      margin-bottom: var(--spacing-lg);
      padding-bottom: var(--spacing-lg);
    }

    .footer-links-row {
      flex-direction: column;
      gap: var(--spacing-sm);
    }
  }
</style>

<script>
(function() {
  const form = document.getElementById('newsletter-form');
  const messageEl = document.getElementById('newsletter-message');
  const submitBtn = document.getElementById('newsletter-submit');
  const emailInput = document.getElementById('newsletter-email');

  if (!form) return;

  form.addEventListener('submit', async function(e) {
    e.preventDefault();
    
    const email = emailInput.value.trim();
    if (!email) return;

    // Disable submit button
    const originalText = submitBtn.textContent;
    submitBtn.disabled = true;
    submitBtn.textContent = 'SUBSCRIBING...';

    try {
      // Method 1: Try to create customer account (appears in Customers tab)
      // Generate a random password for account creation
      const randomPassword = Math.random().toString(36).slice(-12) + Math.random().toString(36).slice(-12);
      
      const customerData = new URLSearchParams();
      customerData.append('form_type', 'customer');
      customerData.append('utf8', '✓');
      customerData.append('customer[email]', email);
      customerData.append('customer[password]', randomPassword);
      customerData.append('customer[password_confirmation]', randomPassword);
      customerData.append('customer[accepts_marketing]', 'true');
      customerData.append('customer[tags]', 'newsletter,interested');

      const customerResponse = await fetch('/account', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/x-www-form-urlencoded',
          'Accept': 'text/html,application/xhtml+xml'
        },
        body: customerData,
        redirect: 'manual'
      });

      // If customer creation succeeds (or email already exists), show success
      if (customerResponse.status === 200 || customerResponse.status === 302 || customerResponse.status === 0) {
        messageEl.textContent = 'Thank you for subscribing!';
        messageEl.className = 'newsletter-message success';
        messageEl.style.display = 'block';
        emailInput.value = '';
        
        setTimeout(() => {
          messageEl.style.display = 'none';
        }, 5000);
      } else {
        // Fallback: Use contact form (emails will be in contact form submissions)
        const contactData = new URLSearchParams();
        contactData.append('form_type', 'contact');
        contactData.append('utf8', '✓');
        contactData.append('contact[email]', email);
        contactData.append('contact[body]', 'Newsletter signup - Please add to customer list');
        contactData.append('contact[tags]', 'newsletter');

        const contactResponse = await fetch('/contact', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/x-www-form-urlencoded',
            'Accept': 'application/json'
          },
          body: contactData
        });

        if (contactResponse.ok) {
          messageEl.textContent = 'Thank you for subscribing!';
          messageEl.className = 'newsletter-message success';
          messageEl.style.display = 'block';
          emailInput.value = '';
          
          setTimeout(() => {
            messageEl.style.display = 'none';
          }, 5000);
        } else {
          throw new Error('Subscription failed');
        }
      }
    } catch (error) {
      console.error('Newsletter signup error:', error);
      messageEl.textContent = 'Something went wrong. Please try again.';
      messageEl.className = 'newsletter-message error';
      messageEl.style.display = 'block';
      
      setTimeout(() => {
        messageEl.style.display = 'none';
      }, 5000);
    } finally {
      submitBtn.disabled = false;
      submitBtn.textContent = originalText;
    }
  });
})();
</script>

{% schema %}
{
  "name": "Footer",
  "settings": [
    {
      "type": "header",
      "content": "Newsletter Settings"
    },
    {
      "type": "checkbox",
      "id": "show_newsletter",
      "label": "Show newsletter signup",
      "default": true
    },
    {
      "type": "text",
      "id": "newsletter_title",
      "label": "Newsletter title",
      "default": "JOIN OUR NEWSLETTER"
    },
    {
      "type": "text",
      "id": "email_placeholder",
      "label": "Email placeholder",
      "default": "EMAIL ADDRESS"
    },
    {
      "type": "text",
      "id": "subscribe_button",
      "label": "Subscribe button text",
      "default": "SUBSCRIBE"
    },
    {
      "type": "checkbox",
      "id": "show_disclaimer",
      "label": "Show disclaimer",
      "default": false
    },
    {
      "type": "textarea",
      "id": "disclaimer_text",
      "label": "Disclaimer text",
      "default": "By subscribing, you agree to receive marketing emails. You can unsubscribe at any time."
    },
    {
      "type": "header",
      "content": "Brand Information"
    },
    {
      "type": "textarea",
      "id": "brand_description",
      "label": "Brand description",
      "default": "KWONE BLENDS FUNCTIONALITY AND ELEGANCE, CREATING UNIQUE, PREMIUM PIECES CRAFTED WITH PRECISION."
    },
    {
      "type": "textarea",
      "id": "brand_tagline",
      "label": "Brand tagline",
      "default": "AND REMEMBER: QUALITY OVER QUANTITY."
    },
    {
      "type": "image_picker",
      "id": "footer_logo",
      "label": "Footer logo",
      "info": "Optional logo or brand mark for footer"
    },
    {
      "type": "text",
      "id": "location_text",
      "label": "Location text",
      "default": "YOUR CITY, STATE"
    },
    {
      "type": "header",
      "content": "Social Media"
    },
    {
      "type": "checkbox",
      "id": "show_social",
      "label": "Show social media",
      "default": true
    },
    {
      "type": "text",
      "id": "social_title",
      "label": "Social title",
      "default": "FOLLOW US"
    },
    {
      "type": "text",
      "id": "instagram_handle",
      "label": "Instagram handle",
      "info": "Enter handle without @ (e.g., 'kwone' for @kwone)"
    },
    {
      "type": "text",
      "id": "instagram_label",
      "label": "Instagram label",
      "default": "INSTAGRAM"
    },
    {
      "type": "header",
      "content": "Footer Links"
    },
    {
      "type": "checkbox",
      "id": "show_footer_links",
      "label": "Show footer links",
      "default": true
    },
    {
      "type": "url",
      "id": "privacy_policy_url",
      "label": "Privacy Policy URL"
    },
    {
      "type": "text",
      "id": "privacy_policy_label",
      "label": "Privacy Policy label",
      "default": "PRIVACY POLICY"
    },
    {
      "type": "url",
      "id": "shipping_url",
      "label": "Shipping & Returns URL"
    },
    {
      "type": "text",
      "id": "shipping_label",
      "label": "Shipping & Returns label",
      "default": "SHIPPING & RETURNS"
    },
    {
      "type": "header",
      "content": "Copyright"
    },
    {
      "type": "text",
      "id": "copyright_text",
      "label": "Copyright text",
      "default": "KWONE"
    },
    {
      "type": "checkbox",
      "id": "show_site_credit",
      "label": "Show site credit",
      "default": false
    },
    {
      "type": "text",
      "id": "site_credit_text",
      "label": "Site credit text",
      "default": "SITE BY YOUR NAME"
    }
  ]
}
{% endschema %}
